{
  "name": "blog",
  "version": "1.0.0",
  "description": "Personal blog",
  "scripts": {
    "serve": "yarn run build&&docfx serve _site",
    "deploy": "yarn run build&&firebase deploy --non-interactive",
    "build": "docfx build -t node_modules/docfx-templates-basicblog/dist"
  },
  "author": "JeremyTCD",
  "license": "MIT",
  "devDependencies": {
    "firebase-tools": "^3.5.0"
  },
  "dependencies": {
    "docfx-templates-basicblog": "^0.1.11-beta"
  }
}
